# Web application (app.py) — always required
streamlit>=1.32
pandas>=2.0
numpy>=1.24
plotly>=5.18
openpyxl>=3.1

# Inference layer (backend/inference/) — required ONLY for real scoring via
# the "Advanced: full-feature scoring" panel on the Data Input page. The
# app runs fine without these; that panel will just report the models as
# unavailable and explain why. Demo Mode and basic upload validation never
# need these.
lightgbm>=4.0
xgboost>=2.0
joblib>=1.3
pyarrow>=14.0
